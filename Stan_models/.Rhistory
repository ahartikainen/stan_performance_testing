source("./gp-fit-ARD.data.R")
library(rstan)
x
as.matrix(x)
x = as.matrix(x)
y
as.vector(y)
y = as.vector(y)
list(x=x, y=y)
list(x=x, y=y, N=N, D=D)
stan_rdump(list(x=x, y=y, N=N, D=D), "ard_stan_data.data.R")
colnames(x) <- NULL
x
sort(x)
rownames(x) <- NULL
x
y
stan_rdump(list(x=x, y=y, N=N, D=D), "ard_stan_data.data.R")
stan_rdump(c("x", "y", "N", "D"), "ard_stan_data.data.R")
q()
